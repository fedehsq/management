<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?selectableItemBackground">

    <TextView
        android:id="@+id/simple_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="32dp"
        android:text="@string/text_example"
        android:textAppearance="?textAppearanceListItem"
        app:layout_constraintBottom_toBottomOf="@id/divider_image_view"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <ImageButton
        android:id="@+id/delete_image_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_baseline_delete_forever_24"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/simple_text_view"
        app:layout_constraintBottom_toBottomOf="@id/simple_text_view"
        android:layout_marginEnd="32dp"
        />

    <ImageView
        android:layout_margin="32dp"
        android:id="@+id/divider_image_view"
        app:layout_constraintTop_toBottomOf="@id/simple_text_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/line"
        >

    </ImageView>

</androidx.constraintlayout.widget.ConstraintLayout>